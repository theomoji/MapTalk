<%= @user.name %>
<br>
<% @user.images.each do |image|%>
<%= image.title %>
<%= image_tag "#{image.img_url}", style: 'height:10%;width:10;'  %>
<% image.hashtags.each do |tag|%>
<%= tag.name %>


<%image.comments.each do |comment| %>

Comments:<%= comment.content %>
<% end %>
<% end %>
<% end %>
